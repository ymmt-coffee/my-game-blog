# my-game-blog 作業ルール

このファイルは、このリポジトリ内で作業するAIエージェント向けの共通指示である。Phase固有のプロンプトと併せて適用する。

## 最初に読む資料

作業開始前に、最低限次を確認する。

1. `docs/automation-common-rules.md`
2. `docs/README.md`
3. `docs/blog_automation_spec.md`
4. `docs/roadmap.md`
5. `data/editorial/strategy.yaml`
6. `docs/publishing-workflow.md`
7. 対象Phaseの直前までの実装報告

ユーザーが追加の確認資料を指定した場合は、それもすべて確認する。

## 判断の優先順位

- 現在のコード、テスト、`docs/publishing-workflow.md`、最新の実装報告を過去の監査記録より優先する。
- 方針の意図は `docs/blog_automation_spec.md`、確定値は `data/editorial/strategy.yaml` で確認する。
- 資料同士、または資料と実装が矛盾する場合は、影響を確認し、ユーザー判断が必要な内容を勝手に決めない。
- モデル、サービス、ショートカット、Secret名、外部通知条件など変更され得る値は、このファイルへ重複記載しない。

## 必ず守る安全策

- ユーザーの未コミット変更を変更、破棄、上書き、巻き戻ししない。
- Obsidian原稿の本文、画像、個人メモを、明示された処理以外で変更、移動、削除しない。
- `review-report.md` を同期、commit、公開対象へ含めない。
- 独立した `my-blog` とその公開操作を、明示依頼なしに変更しない。
- PaperMod submodule内を直接変更しない。
- APIキー、Webhook URL、token、Cookie、認証情報、非公開の個人情報を表示、保存、commit、通知しない。
- 架空のSecret、APIキー、Webhook URL、モデルID、料金を設定しない。
- テストから外部AI、Discord、SNSなどの実サービスを呼ばない。fakeまたはstubを使う。
- 現在対象のPhaseより後の機能を先行実装しない。
- 外部サービスの変更、実送信、Obsidian設定変更、commit、push、公開は、ユーザーの明示承認範囲でだけ行う。

## 作業の進め方

- 作業開始時にbranch、remote、作業ツリー、既存変更、現在のテストとHugoビルドを確認する。
- 読み取り専用の調査は先に進め、重要な設計判断や外部変更は実装前に推奨案と選択肢を示す。
- 実装は必要最小限の範囲に限定し、既存の安全なプレビュー・公開・校正処理を後退させない。
- 失敗途中のファイルを残さず、可能な処理は一時ファイルと原子的置換を使う。
- 外部入力やAI出力をコマンドとして実行しない。
- 変更後は関連テスト、既存回帰テスト、Hugoビルド、Git差分を確認する。
- commitする場合は対象Phaseのファイルだけをstageし、対象一覧を検証する。

## 資料と報告

- 方針や現在状態を変えた場合は、`docs/automation-common-rules.md` の資料同期ルールに従う。
- 過去Phaseの監査・実装報告は当時の記録として扱い、現在値へ一律に書き換えない。
- Phaseの結果は専用の実装報告へ残し、決定事項、保留事項、テスト、外部変更を明記する。
- 完了報告は非エンジニアにも判断できる言葉で、実装内容、安全策、テスト、保留事項、commit・push・公開状態を説明する。
