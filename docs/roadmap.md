# ローカル管理画面 再構築ロードマップ

## 現在地

2026-08-09に旧Phase方式を停止し、管理画面中心の計画へ移行した。Hugoブログ、校正、プレビュー、公開、検査、バックアップは維持している。Discordと週次AI調査は保留した。

## Phase A: データ構造と移行設計

状態: 2026-08-09に設計5項目のユーザー承認を受けて完了。詳細は `phase-a-data-migration-design.md`。

- 記事と画像の正本を決める
- 既存記事と外部原稿の移行方法を決める
- 記事状態、予約、履歴のSQLiteスキーマを決める
- 自動保存、復元、アーカイブ方針を決める
- 管理画面から既存公開処理を呼ぶ境界を決める

完了条件: ファイル配置、状態遷移、移行、失敗時の復元方法が文書とテスト案で確定している。

確定事項: 正本は `content/articles/`、技術構成はFastAPIと通常HTML/CSS/JavaScript、自動保存は入力後2秒かつ遅くとも30秒、履歴は30世代または90日、アーカイブは論理方式、旧原稿は移行後30日かつ次回月次バックアップ完了まで保持する。Phase B/Cの実装や原稿移行は、各Phaseの作業依頼に基づいて開始する。

## Phase B: 管理画面の土台

状態: 2026-08-09に実装・ローカル検証完了。詳細は `phase-b-admin-shell-report.md`。

- localhost限定のローカルWebアプリ
- 固定左メニューと画面切替
- 設定・履歴・エラー表示の基礎
- 未実装画面の準備中表示

完了条件: 外部公開されず、起動と終了が簡単で、記事管理画面へ移動できる。

確認結果: `127.0.0.1` 固定、未許可Host拒否、単一起動、固定メニュー、設定・履歴、エラー表示の基礎、準備中画面、起動・終了手順を実装した。記事管理はPhase Cまで読み取り・書き込みとも有効化しない。

## Phase C: 記事管理

状態: 2026-08-09に実装・ローカル検証完了。詳細は `phase-c-article-management-report.md`。

- 記事一覧、新規作成、編集
- 画像追加、並べ替え、説明文
- 自動保存、手動保存、復元
- 状態管理とアーカイブ
- 既存記事の安全な読み込み

完了条件: 管理画面だけで下書きを作成・再編集でき、失敗時に元記事を壊さない。

確認結果: 通常Markdownの新規作成・編集、画像追加、自動保存、確定保存、履歴・復元、複数画面と外部変更の競合停止、論理アーカイブ、DB再構築、既存原稿の読み取り専用dry-runを実装した。既存原稿の実移行、削除、公開は行っていない。

## Phase D: 記事テンプレート

状態: 2026-08-09に実装・ローカル検証完了。詳細は `phase-d-article-templates-report.md`。

- プレイ途中記
- 新作・セール5選
- 月次レビューエッセイ
- 必須項目と記事種類別検査

完了条件: 3種類の記事をフォームから正しいMarkdownとして作成できる。

確認結果: 3カテゴリーの本文雛形、共通必須項目、プレイ途中記のプレイ時間、注意表示、編集画面の検査結果を実装した。日本語・YAML特殊文字・日付・未知キー保持をテストし、カテゴリー変更で既存本文を自動上書きしない。

## Phase E: 校正・プレビュー・公開

状態: 2026-08-09に実装・ローカル検証完了。詳細は `phase-e-publishing-report.md`。

- AI校正と指摘の採否
- Hugoプレビュー
- 公開前チェック
- 最終確認
- 記事単位のcommit、push、Pages確認

完了条件: ObsidianとDiscordを使わず、管理画面だけで記事公開を完了できる。

確認結果: AI校正の本文非変更、指摘の採否、校正鮮度、Hugoプレビュー、公開前検査、15分有効の最終確認、記事限定Git操作、Pagesと公開URL確認を実装した。テストはfake/stubを使用し、実AI・commit・push・公開は行っていない。

現在の運用調整: slugは日付＋日別連番で自動作成し、記事検索、下書き・公開済タブ、設定・履歴から復元できる論理削除を追加した。移行前原稿の確認入口は記事一覧から外した。AI校正はテンポを優先して停止・非表示とし、公開前検査の必須条件から外している。

移行前の外部テスト原稿3件は2026-08-09のユーザー承認によりWindowsのごみ箱へ移動済みで、管理画面の記事と公開用記事は保持している。

## Phase F: ブログデザイン

- 記事一覧と記事ページ
- モバイル表示、読みやすさ、アクセシビリティ
- 広告を後から追加できる限定的な配置場所
- 実記事を使った表示確認

完了条件: 公開ブログのデザインが確定し、広告なしでも不自然な空白を作らない。

ここまでをブログ投稿MVPとする。

## Phase G: 予約とカレンダー

記事画面へ予約を追加した後、月・週カレンダーを実装する。

## Phase H: アクセス解析

外部データ取得、表示、比較、改善提案の順に実装する。

## Phase I: SNS

投稿案、確認、手動投稿、予約、結果取得、自動化の順に実装する。

## Phase J: 共通情報収集

AI編集部とリリース・セール画面が共有する、検証済みゲーム情報の収集基盤を作る。

## Phase K: AI編集部

候補3本、理由、予算、採用・保留・見送り、記事下書き作成を実装する。

## Phase L: リリース・セール情報

候補の絞り込み、公式情報確認、5本選択、記事下書き作成、カレンダー反映を実装する。

## 共通停止条件

秘密情報、記事破損、対象外変更、検査失敗、Hugoビルド失敗、状態不整合、承認不足がある場合は保存または公開を停止する。外部接続、commit、push、公開は個別承認を必要とする。
